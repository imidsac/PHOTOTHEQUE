<%= simple_form_for @banque, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :nom %>
  <%= f.input :compte %>
  <div class="form-actions">
    <%= f.button :submit, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                banques_path, :class => 'btn' %>
  </div>
<% end %>
