<%= simple_form_for [@fournisseur, @paiement], :html => { :class => 'form-horizontal' } do |f| %>
    <%= f.input :datepaiement %>
    <%= f.input :motif %>
    <%= f.input :montant %>
    <div class="form-actions">
      <%= f.button :submit, :class => 'btn btn-primary' %>
      <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                  fournisseur_url(@fournisseur.id, :tout_paiement_fournisseur => true), :class => 'btn' %>
    </div>
<% end %>
