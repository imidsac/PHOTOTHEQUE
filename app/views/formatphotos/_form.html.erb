<%= simple_form_for @formatphoto, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :dimention %>
  <%= f.input :info %>
  <div class="form-actions">
    <%= f.button :submit, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                formatphotos_path, :class => 'btn' %>
  </div>
<% end %>
